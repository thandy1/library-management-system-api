[project]
name = "library-management-system-api"
version = "0.1.0"
description = "A REST API for managing library books and members"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.141.1",
    "uvicorn[standard]==0.53.0",
    "pydantic==2.13.5",
    "email-validator==2.3.0",
]
